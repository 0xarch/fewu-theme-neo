include /mixins/script.pug
include /mixins/type_script.pug
doctype html
html 
    head
        +meta(user.note,user.keyword||i18n('Tags'))
        +useComponent('Neo/Basic')
        +useComponent('Neo/Footer')
        +useComponent('Neo/NavigationBar')
        +useComponent('Neo/PageNavigator')
        title= __title__(i18n('Tags'),user.name)
    body.new(OCP='OVERRIDE')
        +NavigationBar
        +PageLabel(i18n('Tags'))
        main.uWcon
            .cSortCon
                each item in cycling.value
                    +SingleSort(item.value,'tag')
            +PageNavigator(cycling.total,cycling.now,'tag')
        +Footer